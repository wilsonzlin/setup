#!/usr/bin/env bash

set -e

echo "Do NOT use PowerTOP, just use TLP:"
echo "----------------------------------"
echo "1. TLP does everything PowerTOP does, plus a lot more"
echo "2. TLP is a lot more stable, reliable, updated, polished, and optimised"
echo "3. PowerTOP throttles the CPU much harder, and is not as configurable"
echo "4. TLP is very complex and dynamic, handles many edge cases, and is well-tested with many different setups and situations"

exit 1
